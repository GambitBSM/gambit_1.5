# YAML for sterile neutrino scans

Parameters:

  SN_dev:
    M_1:
      range: [0, 2]
    M_2:
      range: [0, 2]
    M_3:
      range: [0, 2]
    ReOm23:
      range: [0, 6.2830]
    ImOm23:
      range: [-8, 8]
    ReOm13:
      range: [0, 6.2830]
    ImOm13:
      range: [-8,8]
    ReOm12:
      range: [0, 6.2830]
    ImOm12:
      range: [-8,8]
    alpha1:
      range: [0, 6.2830]
    alpha2:
      range: [0, 6.2830]
    delta:
      range: [0, 6.2830]
    ordering:
      fixed_value: 1  # 1: normal ordering, 0: inverse ordering
    min_mass:
      fixed_value: 0  # 0: massless, 1: 0.23 eV

Priors:

Printer:

  printer: hdf5
  options:
    output_file: "SN_dev.hdf5"
    delete_file_if_exists: true

Scanner:

  use_scanner: multinest

  scanners:
    multinest:
      plugin: MultiNest
      like: LogLike
      nlive: 50

ObsLikes:

  - capability: SN_stuff
    purpose: Observable
    function: printable_CI
    dependencies:
      - capability: SN_stuff
        type: Matrix3d

  - capability: lnLikelihood
    purpose: LogLike
    function: lnL
    dependencies:
      - capability: SN_stuff
        type: double

Rules:

  - capability: SN_stuff
    type: Matrix3d
    function: CI_param

  - capability: SN_stuff
    type: double
    function: printable_CI

KeyValues:

  likelihood:
    model_invalid_for_lnlike_below: -1e10
    debug: true

  default_output_path: "runs/SN_dev/"
