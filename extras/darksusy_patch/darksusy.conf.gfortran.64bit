#!/bin/bash

patch -N configure $BASH_SOURCE

./configure F77=gfortran FFLAGS="-fPIC -m64" CFLAGS="-fPIC -m64" FOPT="-O -ffixed-line-length-none -m64 -fPIC" CC=gcc

exit

3342c3342
< unset FFLAGS
---
> #unset FFLAGS   Commented out so that -fPIC can be passed to FH to build shared lib for GAMBIT
